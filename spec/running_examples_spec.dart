class RunningExamplesSpec extends SpecMap {
  spec() {
    describe("Running Examples", {

      // Pending while I implement running a single example
      "it can run the examples in a single describe": null,
      //  var describe = new RunningExamplesSpec_SingleDescribe().describes[0];

      "it can run the examples in a single spec": null,

      "it runs examples in all describes (including nested)": null

    });
  }
}
